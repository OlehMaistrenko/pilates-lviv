<?php
/**
 * Закриває розмітку, відкриту в layout-open.php.
 *
 * Чому умова повторюється тут, а не тягнеться одним if через обидва
 * файли: PHP компілює кожен include окремо, тож блок, відкритий в одному
 * файлі, не можна закрити в іншому — «unexpected end of file». Тому
 * кожен партіал балансує свої теги сам, а стан перевіряється двічі.
 */
?>
<?php if (account_is_logged()): ?>
      </div>
    </div>
  </div>
</section>
<?php endif; ?>
